import { ScrollView, Text, View } from "react-native";

export default function home() {
   return (
    <ScrollView>
        <View>
            <Text>hello, world!</Text>
        </View>
    </ScrollView>
   )

}